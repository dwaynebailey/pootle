# Pre-port dependency baseline (Phase 0, stream A2).
#
# Exact, fully-resolved versions of everything requirements/base.txt pulls
# in — including transitive dependencies base.txt doesn't pin directly
# (certifi, requests, six, django-appconf, webassets, etc.) — captured by
# installing base.txt into a clean virtualenv and running `pip freeze`.
# This is the fixed reference point every later Phase 0-5 change gets
# diffed against; see PORTING.md and the audit's Phase 0 deep dive.
#
# Generated: 2026-08-23, tagged as git commit `pre-port-baseline`.
# Environment: Python 2.7.13, inside a debian:stretch-slim container
# (docker/root/install-root, repointed at archive.debian.org since
# stretch is past its own EOL — see stream A1), platform linux/amd64
# under QEMU emulation (this fork's dev machine is arm64).
#
# Known issue surfaced by this freeze, not present in base.txt itself:
# python-Levenshtein==0.12.0 is YANKED on PyPI ("Insecure, upgrade to
# 0.12.1") — pip installs it anyway when a yanked version is explicitly
# pinned, but it should not be treated as a safe baseline. Tracked as
# roadmap Phase 1 item 5 (swap to Levenshtein/rapidfuzz).

Babel==2.5.3
bleach==2.1.3
certifi==2021.10.8
chardet==4.0.0
click==7.1.2
cssmin==0.2.0
diff-match-patch==20121119
dirsync==2.2.3
dj.subcommand==0.0.3
Django==1.11.29
django-allauth==0.35.0
django-appconf==1.0.4
django-assets==0.12
django-bulk-update==2.2.0
django-contact-form==1.5
django-contrib-comments==1.7.3
django-overextends==0.4.3
django-redis==4.10.0
django-rq==1.0.1
django-sortedm2m==1.5.0
django-statici18n==1.7.0
html5lib==1.1
idna==2.10
jsonfield==2.0.2
lxml==4.2.6
makeyfile==0.0.2
Markdown==2.6.11
oauthlib==3.1.0
pathlib==1.0.1
pycountry==18.2.23
python-dateutil==2.7.2
python-Levenshtein==0.12.0
python-openid==2.2.5
pytz==2018.3
redis==3.5.3
requests==2.27.1
requests-oauthlib==1.3.1
rq==0.10.0
scandir==1.7
six==1.17.0
stemming==1.0.1
translate-toolkit==2.3.0
urllib3==1.26.20
webassets==2.0
webencodings==0.5.1
